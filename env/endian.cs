using System;

namespace env
{
    public class Endian
    {
        UInt32 tn_cpu_to_le(UInt32 val)
        {
            return val;
        }

        UInt32 tn_le_to_cpu(UInt32 val)
        {
            return val;
        }
    }
}
